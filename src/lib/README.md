# src/lib

Shared, framework-adjacent utilities and integration code with no UI
(e.g. future Supabase client helpers, formatting/validation utilities,
shared server-side helpers). Business logic that isn't specific to one
feature belongs here rather than inside components.

No modules exist here yet.
