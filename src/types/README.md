# src/types

Shared TypeScript types and interfaces used across more than one feature
(e.g. core domain types such as Person, ParticipationPeriod, once they are
defined). Feature-local types should stay inside their own feature folder
instead.

No shared types exist yet.
