# src/components/ui

Small, generic, reusable UI primitives (buttons, inputs, cards, badges,
etc.) with no feature-specific or business logic. Intended to be the base
design-system layer other components build on.

No components exist here yet.
